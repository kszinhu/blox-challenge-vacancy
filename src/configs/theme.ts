import { createTheme } from "@mui/material/styles";

export const applicationTheme = createTheme({
  palette: {
    primary: {
      main: "#638cf8",
    },
    secondary: {
      main: "#fff",
    },
    info: {
      main: "#edf2ff",
    },
  },
});
