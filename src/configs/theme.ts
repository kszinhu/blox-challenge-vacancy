import { createTheme } from "@mui/material/styles";

export const applicationTheme = createTheme({
  palette: {
    
    primary: {
      main: "#0052cc",
    },
    secondary: {
      main: "#edf2ff",
    },
    info: {
      main: "#edf2ff",
    },
  },
});
