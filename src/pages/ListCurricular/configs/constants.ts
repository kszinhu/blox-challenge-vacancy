export enum ViewOptions {
  grid = "GRID",
  list = "LIST",
  column = "COLUMN",
}

export enum CurricularStatus {
  archived = "archived",
  active = "active",
  pending = "pending",
  accepted = "accepted",
}